# Gammu SMSD Add-on

This add-on provides Gammu SMSD for sending and receiving SMS using Huawei E3372h and similar modems.

## Configuration

- `device`: Path to modem (e.g. /dev/ttyUSB0)
- `pin`: SIM PIN (optional)
- `loglevel`: INFO, DEBUG, ERROR

## Logs

Logs are stored in `/data/smsd.log`.

